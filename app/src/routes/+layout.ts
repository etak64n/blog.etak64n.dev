// Static blog: prerender everything so markdown is processed at build time
// (Node available) and deployed as static assets on Cloudflare Pages.
export const prerender = true;
export const trailingSlash = 'always';
